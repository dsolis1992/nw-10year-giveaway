{
  "framework": null,
  "buildCommand": "mkdir -p dist && cp index.html dist/ && cp og-image.jpg dist/ 2>/dev/null || true",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
